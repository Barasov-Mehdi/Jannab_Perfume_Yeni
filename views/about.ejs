<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Haqqımızda</title>
    <style>
        body,
        html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .full-screen-image {
            position: relative;
            width: 100%;
            height: 100%;
        }

        .full-screen-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .home-link {
            position: absolute;
            top: 0;
            left: 0;
            background-color: rgba(0, 0, 0, 0.7);
            color: #fff;
            text-decoration: none;
            padding: 5px 10px;
            border-radius: 5px;
            font-size: 16px;
            font-family: Arial, sans-serif;
            transition: background-color 0.3s ease;
        }

        .home-link:hover {
            background-color: rgba(255, 255, 255, 0.8);
            color: #000;
        }

        @media (max-width: 768px) {

            body,
            html {
                margin: 0;
                padding: 0;
                width: 100%;
                height: 100vh;
                display: flex;
                align-items: center;
                justify-content: center;
            }
        }
    </style>
</head>

<body>

    <div class="full-screen-image" id="imageContainer">
        <a class="home-link" href="/">Əsas səhifə</a>
        <img id="responsiveImage" src="" alt="Haqqımızda">
    </div>

    <script>
        function loadImage() {
            const responsiveImage = document.getElementById('responsiveImage');
            const width = window.innerWidth;

            // Mobil ve büyük ekranlar için uygun resmi seç
            responsiveImage.src = width <= 768
                ? "/img/HaqqımızdaMB.png"  // Mobil boyutlar
                : "/img/HaqqımızdaPC.png"; // Büyük ekranlar
        }

        // Sayfa yüklendiğinde ve pencere boyutu değiştiğinde resmi yükle
        window.addEventListener('load', loadImage);
        window.addEventListener('resize', loadImage);
    </script>

</body>

</html>